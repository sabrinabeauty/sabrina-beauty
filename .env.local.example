# Optional. If unset, a session secret is generated automatically in memory
# (existing admin sessions just expire on server restart, which is fine for a
# single-operator admin panel). Set this in production if you want sessions to
# survive restarts/deploys.
SESSION_SECRET=
